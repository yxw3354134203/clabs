#include<stdio.h>
   int max (unsigned n)
   unsigned long long int;
{
    int a;
    {
        n++;
    }
    return a;
}
int main()
{
    unsigned int y;
    while (y>0)
    {
        y++;
    }

printf("Max Bit:%llu:",main(1));
printf("Min Bit:%llu:",main(1));
return 0;
}